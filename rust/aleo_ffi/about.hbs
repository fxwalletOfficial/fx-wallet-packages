# Third-party licenses

The prebuilt `aleo_rust` native library statically links the Rust crates below
(snarkVM and its transitive dependencies). Generated by `cargo about generate`
from `rust/aleo_ffi/about.toml`; the `aleo_ffi` crate itself is Apache-2.0.

## License summary

{{#each overview}}
- {{{name}}}: {{count}}
{{/each}}

{{#each licenses}}
## {{{name}}}

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} — {{{crate.repository}}}{{/if}}
{{/each}}

```
{{{text}}}
```

{{/each}}
